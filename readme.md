# 🌾 Crop Recommendation System

A simple Flask-based web application that recommends the best crop to plant based on soil nutrients and weather conditions.

## 🚀 Setup

1. Clone the repository or extract the folder.
2. Place your trained model file (`crop_classification_model.pkl`) in the main directory.
3. Install dependencies:
   ```bash
   pip install -r requirements.txt
